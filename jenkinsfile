pipeline {
    agent any

    triggers {
        githubPush() // Se activa con cada push a GitHub
    }

    stages {
        stage('Trigger from GitHub') {
            steps {
                echo 'Pipeline triggered by GitHub push'
            }
        }
    }
}